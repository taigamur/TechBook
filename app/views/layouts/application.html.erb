<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type"/>
    <title>MyBookApp</title>
      <%= favicon_link_tag('favicon.ico') %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  
    <header>
      <% if current_user %>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
          <a class="navbar-brand" href=<%= root_path%>>BookHub</a>
          <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav mr-auto">
  
              <li class="nav-item">
                <a class="nav-link" href=<%= user_path(id: current_user.id)%>>MyPage</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href=<%= users_path %>>All Users</a>
              </li>
              <li class="nav-item">
                  <a class="nav-link" href='#'>BookMark</a>
              </li>
              <li class="nav-item">
                  <a class="nav-link" href=<%= new_search_path %>>New Post</a>
              </li>
              <li class="nav-item">
                  <a class="nav-link" href=<%= posts_path %>>Show Index</a>
              </li>
            </ul>
            <div class="btn-group">
              <button type="button" class="btn btn-link" data-toggle="dropdown" aria-expanded="false">
                <svg xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="currentColor" class="bi bi-list" viewBox="0 0 16 16">
                  <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"/>
                </svg>
              </button>
              <div class="dropdown-menu dropdown-menu-right">
                <%= link_to "MyProfile", home_path(id: current_user.id), class: "dropdown-item" %>
                <%= link_to "MyPage", user_path(id: current_user.id), class: "dropdown-item" %>
                <%= link_to "Logout", log_out_path, class: "dropdown-item" %>  <!-- , data: { confirm: "ログアウトしますか？"} -->
              </div>
            </div>
          </div>
        </nav>
      <% else %>

      <% end %>
    </header>
  
  <body>

      <div class="container main">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-success"><%= message %></div>
      <% end %>
      </div>
      
      <div class="container">
      <%= yield %>
      </div>

  </body>

  <footer>

  </footer>
</html>
